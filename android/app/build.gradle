plugins {
    id 'com.android.application'
}

def keystorePropertiesFile = rootProject.file('keystore.properties')
def keystoreProperties = new Properties()
if (keystorePropertiesFile.exists()) {
    keystoreProperties.load(new FileInputStream(keystorePropertiesFile))
}

android {
    namespace 'com.cortexdevelopments.rootweave'
    compileSdk 36

    defaultConfig {
        applicationId "com.cortexdevelopments.rootweave"
        minSdk 21
        targetSdk 36
        versionCode 3
        versionName "1.0.2"
        resValue "string", "appName", "Rootweave"
    }

    signingConfigs {
        release {
            if (keystorePropertiesFile.exists()) {
                storeFile rootProject.file(keystoreProperties['storeFile'])
                storePassword keystoreProperties['storePassword']
                keyAlias keystoreProperties['keyAlias']
                keyPassword keystoreProperties['keyPassword']
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            if (keystorePropertiesFile.exists()) {
                signingConfig signingConfigs.release
            }
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    // 2.6.1 LauncherActivity.launchTwa calls setLaunchHandlerClientMode (browser 1.9.0+).
    // Pinning browser 1.8.0 with this exclude crashed closed testers (NoSuchMethodError).
    implementation('com.google.androidbrowserhelper:androidbrowserhelper:2.6.1') {
        exclude group: 'androidx.browser', module: 'browser'
    }
    implementation 'androidx.browser:browser:1.9.0'
    implementation 'androidx.appcompat:appcompat:1.7.0'
}
